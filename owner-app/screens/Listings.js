import { View, Text, Button, TextInput } from "react-native"

const Listings = ({navigation}) => {
    return(
        <View>
            <Button title="Create Listing">Create Listing +</Button>
            <Text>Testing Listings</Text>
        </View>
    )
}

export default Listings